# ShipFlow 协作约束

- 每次只实现一个模块。
- 编码前先解释业务流程，明确参与方、状态变化、业务规则和异常路径。
- 金额使用 `BigDecimal`。
- 时间必须明确时区。
- 所有写接口都要考虑幂等、事务和重复请求。
- 所有业务表都要考虑 `tenant_id`。
- 核心测试同时进行接口断言和数据库断言。
- 异步业务使用轮询验证，禁止固定长时间 `sleep`。
- 修改后必须运行真实测试。
- 测试失败不能伪造通过。
- 禁止提交密码、Token 和真实客户数据。
- 每完成一个模块生成学习笔记。
- 完成后向项目负责人提出业务和技术问题。
