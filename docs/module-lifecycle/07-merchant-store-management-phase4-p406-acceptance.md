# P4-06 店铺主数据与基础资料验收

更新时间：2026-08-19（Asia/Shanghai）

## 门禁结果

| 项目 | 结果 | 说明 |
|---|---|---|
| 自动匹配 | PASS（0） | 当前没有可安全自动匹配的对象 |
| `STORE_JP_001` 归属 | BLOCKED | 租户归属不唯一 |
| `STORE_EU_001` 迁移 | PASS | 明确禁止从 `TENANT_DEMO_002` 迁移 |
| 平台映射 | BLOCKED | `MARKETPLACE_A/B` 未获批准映射 |
| `warehouse` | BLOCKED | 正式结构不存在，需独立结构迁移 |
| `region_carrier_relation` | BLOCKED | 正式结构不存在，需独立结构迁移 |
| V021 | SKIPPED/BLOCKED | 未创建、未执行，仅规划为纯数据回填 |
| 本轮数据库业务数据写入 | PASS（0） | 未执行 DML |

## 通过条件

业务确认五项主数据，DBA 批准两个独立结构迁移及备份/回滚方案，并批准 V021 的可靠来源、范围和测试库执行权限后，才可重新进入 P4-06。
## 最终差异处理验收门禁（2026-08-19）

| 项目 | 结果 |
|---|---|
| `STORE_JP_001` 唯一归属和重复记录处理 | BLOCKED，待业务确认 |
| `STORE_EU_001` 保持`TENANT_DEMO_002` | PASS，待纳入跨租户404 |
| 平台/渠道差异 | BLOCKED，5条权威渠道均未匹配 |
| `warehouse`、`region_carrier_relation` | BLOCKED，需独立结构迁移 |
| V021 | BLOCKED，未创建/未执行，仅允许纯数据回填 |
| 订单/报价/轨迹/快照保护 | PASS，基线7/14/20/14未改变 |

P4-06 不具备进入条件。
## P4-06 运行时复验收口（2026-08-19）

| 范围 | 结果 | 证据/边界 |
|---|---|---|
| 工作台 | PASS | 真实渲染；指标、待办、风险提醒来自工作台 API |
| 店铺列表 | PASS | /app/stores 真实显示 2 个授权店铺；query 恢复和刷新保持通过 |
| 店铺详情 | PASS | 真实详情显示脱敏平台账号；地区、默认地址、默认渠道按接口空值和 unavailableFields 显示“功能暂不可用” |
| 物流资料 | PASS | 真实只读渠道列表显示 4 条现有渠道；未创建或伪造渠道 |
| 报价 | PASS | 页面真实非空数据渲染，无控制台 error/warning 证据 |
| 订单 | PASS | 真实非空订单列表；工作台待确认费用跳转携带 PENDING_FEE_CONFIRMATION，刷新后 query 保留并显示真实筛选结果 |
| 轨迹 | PASS/未查询 | 页面真实渲染空查询状态；未人为构造订单号，未把空查询宣称为轨迹接口通过 |
| 异常与索赔 | PASS | 真实空数据状态，未伪造异常记录 |
| 账单与对账 | PASS/DEFERRED | 真实空数据状态；费用调整查询继续提示后端契约缺失 |
| 仓库入口 | PASS（权限边界） | 当前商户业务人员真实返回 403“无权访问”，未放宽仓库权限 |
| 工作台入口 | PASS | 实际点击指标、待办、风险提醒；分别验证订单列表、订单详情和 query 回显 |
| 数据库迁移 | DEFERRED/BLOCKED | V021/V022 未创建、未执行；本阶段数据库写入 0 |

### 自动化验证

- 前端单元测试：PASS，76/76；
- 前端构建：PASS；
- 后端 JDK 21.0.11 定向测试：PASS，152/152，含 Controller、Service、Mapper/XML、工作台、订单、仓库、轨迹、异常、账单；
- 后端全量测试：PASS，JDK 21.0.11 下 409/409 通过；此前 3 个全局 Context 测试的 `StoreResourceMapper` Bean 注册问题已补齐测试上下文并复验通过；
- OpenAPI 静态检查：PASS，110 个 operationId，110 个唯一；
- 敏感信息扫描：PASS，未发现匹配；
- P4-06 目标文件未完成标记扫描：PASS，未发现实际占位内容；todo 仅为业务字段/测试名称；
- git diff --check：待文档更新后执行。

### 未执行或环境限制

401、网络失败、refresh 失败清理、Cookie 面板属性和需要人为破坏会话的场景未执行，继续标记 BLOCKED/环境限制；不伪造浏览器结果。未授权跨租户 404 沿用既有真实边界证据，若浏览器客户端拦截则保持 BLOCKED。

结论：P4-06 在“现有真实契约和只读交互”范围内可以结束；数据库资源模型和后续迁移不属于本阶段完成项。
