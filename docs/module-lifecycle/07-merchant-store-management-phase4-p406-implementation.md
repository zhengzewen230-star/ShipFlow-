# P4-06 店铺主数据与基础资料实施记录

更新时间：2026-08-19（Asia/Shanghai）

## 当前结果

`BLOCKED`。本轮仅完成静态对账文档收尾：自动匹配 0 条，数据库业务数据写入 0 条。没有执行 V021、DDL、DML 或 Flyway，没有修改 Controller、Service、Mapper/XML、前端、权限数据及历史业务数据。

## 已确认事实

- `STORE_JP_001` 租户归属不唯一；`STORE_EU_001` 保留在 `TENANT_DEMO_002`。
- `MARKETPLACE_A/B` 与业务平台编码没有批准映射。
- `warehouse`、`region_carrier_relation` 缺失。
- V021 不能替代缺失的结构迁移，只能在前置确认完成后执行纯数据回填。

## 后续实施顺序

先完成业务和 DBA 主数据确认，再分别实施 `warehouse` 结构迁移、`region_carrier_relation` 结构迁移，最后经独立批准执行 V021 回填。每一步均需验证外键、唯一约束、幂等、审计、回滚边界及 tenant/store scope。
## 最终差异处理实施边界（2026-08-19）

本阶段未执行任何 DDL、DML、Flyway 或业务代码修改。不得通过跨租户`ON DUPLICATE KEY UPDATE`、删除重复店铺或猜测平台映射解除阻塞。

建议顺序固定为：业务/DBA确认 → 独立`warehouse`结构迁移 → 独立`region_carrier_relation`结构迁移 → 仅纯数据回填的 V021 → 后端契约 → 前端验收。所有回填必须带变更批次、明确主键范围、幂等键和审计记录；无条件 DELETE 不可作为回滚方案。
## P4-06 现有契约实施收口（2026-08-19）

本阶段只使用现有真实 Schema、接口和数据，不等待或执行数据库迁移。

已完成：

- 店铺详情对未配置国家/地区、默认发货地址、默认物流渠道显示真实不可用状态；
- 店铺详情路由参数变化会重新读取真实详情；
- 店铺列表和物流基础资料页面保持真实 API、权限边界、加载/空数据/失败状态；
- 工作台入口实际跳转到订单、仓库、轨迹、异常和账单路由，query 白名单和非法筛选提示保持；
- 不支持的聚合筛选显示中文提示，不进行前端假过滤；
- 未改动租户/店铺权限、订单及历史快照。

本阶段未执行：V021/V022、DDL、DML、Flyway、warehouse/region_carrier_relation 创建、重复店铺归档或业务代码的大范围重写。店铺默认地址、默认渠道、仓库主数据和地区关系登记为 DEFERRED/BLOCKED。
