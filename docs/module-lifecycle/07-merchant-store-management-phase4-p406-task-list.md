# P4-06 店铺主数据与基础资料验收任务清单

更新时间：2026-08-19（Asia/Shanghai）

## 当前状态

P4-06：BLOCKED，尚未开始写操作或实现。

## 进入前置条件

1. 业务确认 `STORE_US_001`、`STORE_JP_001`、`STORE_EU_001` 的正式租户归属；不得自动迁移跨租户店铺。
2. 业务/平台确认 `MARKETPLACE_A` 与 AMAZON、RAKUTEN、SHOPIFY 的正式编码映射；不得猜测映射。
3. DBA 提供并批准 `warehouse` 主数据 Schema、外键、索引、审计和回滚方案。
4. DBA/业务提供并批准 `region_carrier_relation` 的语义、字段、租户/店铺归属和迁移版本；现有 `logistics_channel_service_country` 不能直接替代。
5. 明确 5 个物流渠道的正式编码、承运商、国家范围、材积重、时效、货物属性和计费规则，并确认店铺绑定关系。
6. V021 回填范围、幂等策略、备份、回滚脚本和测试库执行权限全部批准。

## 已确认限制

- 当前只读数据库为 `shipflow`，Flyway V020。
- 当前 `merchant_store_address`、`merchant_store_channel` 均为 0 行。
- 当前未执行 V021、DDL、DML 或生产调用；历史订单、轨迹、账单和快照未修改。

## 后续实施顺序

先完成结构迁移和主数据对账，再创建纯数据回填 V021；回填成功后验证租户/店铺 scope、404 越权、幂等、审计及历史数据不变，最后再考虑 P4-06 页面验收。
## 主数据对账收尾（2026-08-19）
- 自动匹配数量：0；本轮数据库业务数据写入：0。
- `STORE_JP_001` 租户归属不唯一；`STORE_EU_001` 属于 `TENANT_DEMO_002`，不得迁移。
- `MARKETPLACE_A/B` 与 `AMAZON`、`RAKUTEN`、`SHOPIFY` 尚无批准映射。
- `warehouse` 和 `region_carrier_relation` 不存在；不得用 V021 代替结构迁移。
- V021 只能在主数据确认后承担纯数据回填。
## 推荐拆分
1. 独立结构迁移：`warehouse`；
2. 独立结构迁移：`region_carrier_relation`；
3. V021：仅回填店铺地址、店铺渠道和地区关系。
当前状态：`BLOCKED`，不进入 P4-06 实现或页面验收。
## 确认版任务门禁（2026-08-19）

- [ ] 业务确认 `STORE_JP_001` 唯一租户，确认 `STORE_EU_001` 保持 `TENANT_DEMO_002`；
- [ ] 批准平台映射、3 个店铺商家归属、渠道属性/目的地区域/店铺绑定；
- [ ] 确认 `WH_SZ_01` 仓库归属和负责人；
- [ ] DBA 确认两个结构 Schema、版本、复合外键、唯一索引、初始化来源、备份回滚和执行权限；
- [ ] 独立实施 `warehouse` 结构迁移；
- [ ] 独立实施 `region_carrier_relation` 结构迁移；
- [ ] 仅在前置条件全部满足后创建并执行 V021 纯数据回填。

当前自动匹配 0、业务数据写入 0，P4-06 保持 `BLOCKED`。
## 最终差异处理任务（2026-08-19）

- [ ] 业务确认`STORE_JP_001`唯一归属及删除/停用/保留策略；
- [ ] 业务确认`STORE_EU_001`保持`TENANT_DEMO_002`并纳入跨租户404；
- [ ] 批准平台映射，禁止猜测`MARKETPLACE_A/B`；
- [ ] 确认5条渠道的编码、承运商、区域、计费规则和店铺绑定；
- [ ] DBA确认`warehouse`与`region_carrier_relation`独立结构迁移或正式替代契约；
- [ ] 确认批次回滚、备份、主键范围和测试库执行权限；
- [ ] 前置条件全部满足后才创建 V021 纯数据回填。

当前不可执行：删除/停用重复店铺、写入未确认渠道、创建缺失表、执行 V021。
## P4-06 收口状态（2026-08-19）

| 子项 | 状态 | 说明 |
|---|---|---|
| 现有店铺/物流资料真实展示 | PASS | 保持后端真实接口和 scope 校验 |
| 订单、报价、轨迹、异常、账单页面只读复核 | PASS | 加载、空数据、权限提示和真实跳转已复核 |
| 工作台跳转和 query 回显 | PASS | 指标、待办、风险提醒至少各点击一项；刷新后筛选保持 |
| 未迁移店铺资源 | DEFERRED/BLOCKED | 默认地址、默认渠道、仓库主数据、地区关系不伪造 |
| 数据库迁移 | SKIPPED/BLOCKED | V021/V022 未创建、未执行；数据库写入 0 |
| 后端全量 Context 测试 | PASS | JDK 21.0.11 下全量 409/409 通过；StoreResourceMapper 测试上下文已补齐 |

P4-06 当前可在现有真实契约范围内结束，不进入 V021/V022，不扩展数据库或业务代码。
