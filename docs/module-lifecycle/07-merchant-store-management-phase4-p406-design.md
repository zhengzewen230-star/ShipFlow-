# P4-06 店铺主数据与基础资料设计

更新时间：2026-08-19（Asia/Shanghai）

## 状态

`BLOCKED`。本阶段只完成对账门禁和迁移拆分设计，未执行 V021、DDL、DML、Flyway，未修改业务代码或权限数据。本轮数据库业务数据写入为 0。

## 对账结论

- 自动匹配数量为 0。
- `STORE_JP_001` 的租户归属不唯一，必须由业务确认唯一归属。
- `STORE_EU_001` 属于 `TENANT_DEMO_002`，不得迁移到其他租户。
- `MARKETPLACE_A/B` 与 `AMAZON`、`RAKUTEN`、`SHOPIFY` 尚无批准映射。
- `warehouse` 和 `region_carrier_relation` 不存在；现有仓库作业表和公共渠道服务国家表不能直接替代。
- V021 只能承担纯数据回填，不能建表、修正租户归属或猜测平台映射。

## 待确认事项

### 业务确认

| 编号 | 事项 | 门禁 |
|---|---|---|
| B-01 | `STORE_JP_001` 的唯一租户归属 | 必须确认 |
| B-02 | 数据库平台编码到业务平台编码的映射 | 必须批准 |
| B-03 | 三个业务店铺是否同属一个商家 | 必须确认 |
| B-04 | 五个渠道的权威编码和完整属性 | 必须确认 |
| B-05 | `WH_SZ_01` 的仓库主数据归属 | 必须确认 |

### DBA 确认

| 编号 | 事项 | 门禁 |
|---|---|---|
| D-01 | 是否批准 `warehouse` 结构迁移 | 必须批准 |
| D-02 | 是否批准 `region_carrier_relation` 结构迁移 | 必须批准 |
| D-03 | 字段、复合外键、索引、唯一约束、备份与回滚边界 | 必须批准 |

## 迁移拆分

1. 独立结构迁移：`warehouse`；
2. 独立结构迁移：`region_carrier_relation`；
3. `V021`：仅在主数据来源、租户/店铺归属、平台映射和结构迁移全部批准后，回填 `merchant_store_address`、`merchant_store_channel` 及地区关系。

不得删除重复店铺、自动迁移租户、猜测平台映射或改写历史订单快照。
## 最终差异处理设计（2026-08-19）

P4-06 继续 BLOCKED，当前自动匹配 0、V021 未创建/未执行、数据库业务数据写入 0。

### 业务确认项

- `STORE_JP_001`：确认唯一租户及另一条记录的删除/停用/保留策略；
- `STORE_EU_001`：确认继续保持`TENANT_DEMO_002`，不得迁移；
- 批准`MARKETPLACE_A/B`与业务平台编码的映射；
- 确认 3 个店铺是否同属一个商家；
- 确认 5 条渠道的编码、承运商、目的地区域、计费规则及店铺绑定；
- 确认`WH_SZ_01`仓库归属和负责人。

### DBA 确认项

- `warehouse`独立 Schema、版本、外键、唯一索引和回滚边界；
- `region_carrier_relation`独立 Schema，或对`logistics_channel.service_area/destination_regions`替代方案完成字段契约和兼容性评审；
- 初始化来源、备份点、批次标识和测试库执行权限；
- V021 只允许纯数据回填，不得包含建表语句。

未完成确认前，不进入 V021 或页面验收。
## P4-06 现有真实契约收口（2026-08-19）

本子阶段在不等待、不执行 V021/V022 的前提下完成。当前结论按能力范围区分：现有真实字段、接口和权限边界为 PASS；店铺默认地址、默认渠道、仓库主数据和地区关系为 DEFERRED/BLOCKED，不能用前端假数据补齐。

- 数据库状态保持不变：最高 Flyway 版本 V020；V021/V022 未创建、未执行；本阶段数据库写入 0。
- 继续保留 `tenant_id + user_id + role + active sys_user_store_scope` 和资源归属校验，不恢复 tenant-only 查询。
- 未配置资源继续由详情接口真实返回空值及 `unavailableFields`，前端仅显示“当前未配置”或“功能暂不可用”。
- 本阶段不创建 `warehouse`、`region_carrier_relation`，不自动处理重复店铺，不修改订单、报价、轨迹、账单或历史快照。
