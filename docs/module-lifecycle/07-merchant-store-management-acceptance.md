# 第四阶段店铺管理验收记录

更新时间：2026-08-18（Asia/Shanghai）

## 1. 验收状态

| 项目 | 状态 | 证据 |
|---|---|---|
| 后端列表筛选/分页/排序 | PASS | JDK 21.0.11 定向测试 14/14 |
| 前端 query 解析与安全回退 | PASS | 前端单元测试包含 3 条店铺 query 测试 |
| 前端单元测试 | PASS | 68/68 |
| 前端构建 | PASS | `npm run build` |
| OpenAPI operationId | PASS | 106 个，唯一值 106 |
| diff 格式 | PASS | `git diff --check` |
| 真实登录后店铺筛选 | BLOCKED | 当前受控浏览器无登录态，页面回到登录页 |
| 真实 API 数据与分页回显 | BLOCKED | 未在无登录态下伪造商家验收 |

## 2. 不能宣称通过的项目

本轮没有执行创建、编辑、启停、数据库写入、Flyway、顺丰调用或凭证输入。国家/地区、默认地址、默认渠道和详情字段尚无真实后端契约，不计入 P4-01 完成项。

## 3. 后续复验

在已授权浏览器会话可用后，只读复核：店铺列表、编码/名称/平台/状态筛选、分页、排序、URL 刷新恢复和返回行为，并检查控制台错误；未授权资源继续以真实 404 证据验证。

## P4-02 店铺详情只读验收

| 项目 | 状态 | 证据 |
|---|---|---|
| 详情接口租户/用户/角色/store scope 校验 | PASS | `findByIdForUser` 先于任何聚合查询，未授权服务测试统一 `COMMON-1006/404` |
| 平台账号服务端脱敏 | PASS | 服务测试验证只返回 `platformAccountMasked`，Controller 测试验证原始字段不存在 |
| 历史订单数量真实聚合 | PASS | `shipment_order` 按 `tenant_id + store_id` 统计，服务测试验证聚合调用 |
| 审计摘要真实读取 | PASS | `audit_log` 按租户、资源类型和店铺 ID 查询最近 10 条，Mapper/XML 测试通过 |
| 国家/地区、默认地址、默认渠道 | BLOCKED/契约缺失 | 当前数据库无可靠字段或关联，本阶段返回 `null + unavailableFields`，不伪造 |
| 前端详情路由、query 返回保持、非法 ID | PASS | 详情导航和 service 单元测试通过 |
| 授权浏览器详情页面 | BLOCKED | 当前浏览器是否存在可复用授权会话需由环境提供，未读取或填写凭证 |

本阶段未执行数据库写入、Flyway、顺丰生产调用和 Git 提交；UTC 数据由后端返回，前端使用 Asia/Shanghai 展示。

## P4-03 店铺维护验收

| 项目 | 状态 | 证据 |
|---|---|---|
| 编辑/启停资源归属校验 | PASS | `StoreApplicationServiceTest` 与 `StoreMapperXmlTest`；写操作先走 `findByIdForUser` |
| 编辑幂等与版本冲突 | PASS | Service/Controller 定向测试；`Idempotency-Key` + 请求哈希 + 版本条件更新 |
| 启停幂等与版本冲突 | PASS | Service/Controller 定向测试；OpenAPI 已声明幂等请求头 |
| 停用店铺禁止新订单 | PASS | `ShipmentOrderApplicationServiceTest` 验证 `STORE-1002/422` 且不写订单 |
| 审计与敏感字段 | PASS | 编辑/启停沿用店铺审计；前端不回显原始平台账号，后端详情只返回掩码 |
| 前端权限隐藏、二次确认、防重复、错误 Trace ID | PASS | 前端 74/74 与生产构建；仅 `store:manage` 渲染维护按钮 |
| 真实浏览器编辑/启停 | BLOCKED | 本阶段不执行业务写入，未在浏览器中制造或提交变更 |
| 数据库写入/Flyway/顺丰生产调用/Git 提交 | 未执行 | 阶段约束明确禁止 |

P4-03 自动化证据：JDK 21.0.11 后端定向测试 39/39；前端单元测试 74/74；前端构建通过。真实浏览器的只读列表/详情仍继承 P4-01/P4-02 的环境边界，写操作验收需后续受控测试数据和明确写入授权。

## P4-04 资源模型设计验收

| 项目 | 状态 | 证据 |
|---|---|---|
| 现有表、索引、外键和迁移版本只读核对 | PASS | `database/schema.sql`、V002-V019、订单/物流/店铺 Mapper 和 Service 静态检查 |
| 店铺默认地址真实来源 | BLOCKED | 当前无店铺级地址表；`shipment_address` 仅为订单快照 |
| 店铺默认渠道真实来源 | BLOCKED | 当前无店铺-渠道关联表；`logistics_channel` 为平台公共表 |
| 资源模型和迁移方案 | PASS | P4-04 资源模型与迁移方案文档 |
| API/OpenAPI 实现 | 未执行 | 本阶段只设计，不修改 OpenAPI 或业务代码 |
| 数据库/Flyway | 未执行 | 未创建迁移、未连接数据库、未写入数据 |
| StoreMapper 权限过滤 | PASS | 未修改，继续使用 tenant/user/role/active scope 过滤 |

进入迁移与后端契约实现阶段仍需业务和 DBA 确认，当前不具备执行条件。

## P4-04 迁移前门禁

| 门禁项 | 状态 | 证据/结论 |
|---|---|---|
| 待业务确认规则已列明 | PASS | 已列出默认地址/渠道唯一性、角色读写、停用行为和历史版本策略；尚未批准规则 |
| 待 DBA 确认项已列明 | PASS | 已列出迁移版本、复合外键、生成列/唯一索引、备份和回滚窗口 |
| 计划迁移文件 | V020 PASS，V021 SKIPPED | V020 已执行；V021 无可靠来源，未创建、未执行，回填 0 行 |
| Flyway/数据库写入 | 未执行 | 当前明确禁止，未连接数据库 |
| 现有权限过滤 | PASS | `StoreMapper.xml` 未放宽，继续校验 tenant/user/role/active scope |
| 进入 P4-05 迁移与后端实现 | PASS（范围受限） | V020 已在授权测试库执行；V021 初始化、页面配置写入和浏览器写操作仍未执行 |

## P4-04 业务与 DBA 门禁验收

### 业务确认表

| 事项 | 当前建议 | 状态 |
|---|---|---|
| 默认地址唯一性 | 每店最多一个 ACTIVE 默认地址，可保留历史配置 | 待业务确认 |
| 默认渠道唯一性 | 每店最多一个 ACTIVE 默认渠道，建议可绑定多个渠道 | 待业务确认 |
| 角色权限 | 业务员只读；租户管理员维护；平台管理员维护公共渠道 | 待业务确认 |
| 停用行为 | 停用对象不得用于新报价/新订单，历史快照保留 | 待业务确认 |
| 历史配置版本 | 版本号、审计追加、不物理删除 | 待业务确认 |
| 地区来源 | ACTIVE 默认地址国家编码 | 待业务确认 |

### DBA 确认表

| 事项 | 设计要求 | 状态 |
|---|---|---|
| 迁移版本 | V020 已落地，V021 未创建 | 目标库迁移前 V019，迁移后 V020 |
| Schema 和复合外键 | 确认 `merchant_store(tenant_id,id)`、外键及既有引用兼容性 | 待 DBA 确认 |
| 唯一约束实现 | 确认默认值唯一、渠道绑定唯一和 MySQL 版本兼容性 | 待 DBA 确认 |
| 执行权限 | 测试库 Flyway 权限、备份、执行窗口和回滚人 | 待 DBA 确认 |
| 初始化来源 | 正式地址/渠道来源、脱敏和审计范围 | 待 DBA/业务确认 |

### P4-05 进入门槛

### P4-05 门禁结论

必须同时满足业务规则确认、DBA Schema/迁移确认、测试库 Flyway 执行权限明确、初始化数据来源批准、回滚方案批准。当前均未形成执行授权，因此 P4-04 为“设计完成、实现阻塞”，不进入 P4-05。
## P4-05 实施补充（2026-08-19）

V020 已在授权测试业务库成功执行，V021 未创建/未执行，业务回填为 0 行。后端资源契约和前端 service 契约已补齐；真实浏览器配置写入、生产数据库、顺丰生产调用保持未执行。请以 `07-merchant-store-management-phase4-p405-acceptance.md` 的 PASS、SKIPPED、BLOCKED 和未执行分类为准。

最新主数据对账发现目标租户/平台/仓库/地区关系不具备自动匹配条件；数据库写入仍为 0，P4-06 不进入。
