# 后端功能收口学习笔记

- 演示版运营首页只提供租户范围内的计数聚合，避免重复创建会绕过既有详情与分页查询的宽泛 CRUD。
- 待办汇总跨订单、对账、异常和索赔表，但每个子查询都使用同一个 JWT `tenant_id`；不返回金额、地址、认证信息或其他敏感明细。
- 聚合接口只读、不写审计，也不改变订单状态、价格、费用调整、复称或账单历史。
- 订单取消接口已在实现中存在，本轮将其补入 OpenAPI，约束仅 `DRAFT/PENDING_INBOUND → CANCELLED` 且使用乐观锁与审计。
