<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.shipflow.user.mapper.UserIdempotencyMapper"><resultMap id="recordMap" type="com.shipflow.user.mapper.UserIdempotencyMapper$Record"><constructor><arg column="request_hash" javaType="java.lang.String"/><arg column="resource_id" javaType="java.lang.Long"/><arg column="processing_status" javaType="java.lang.String"/></constructor></resultMap><select id="find" resultMap="recordMap">SELECT request_hash,resource_id,processing_status FROM api_idempotency_record WHERE scope_tenant_id=#{tenantId} AND operation_id=#{operationId} AND idempotency_key=#{key} AND expires_at &gt; CURRENT_TIMESTAMP(3)</select><insert id="insert">INSERT INTO api_idempotency_record (scope_tenant_id,operation_id,idempotency_key,http_method,request_path,request_hash,processing_status,resource_type,expires_at) VALUES (#{tenantId},#{operationId},#{key},'POST','/api/v1/users',#{requestHash},'PROCESSING','user',#{expiresAt})</insert><update id="complete">UPDATE api_idempotency_record SET processing_status='SUCCEEDED',resource_id=#{resourceId} WHERE scope_tenant_id=#{tenantId} AND operation_id=#{operationId} AND idempotency_key=#{key} AND processing_status='PROCESSING'</update></mapper>
