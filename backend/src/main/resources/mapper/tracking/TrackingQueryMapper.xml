<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.shipflow.tracking.mapper.TrackingQueryMapper"><resultMap id="event" type="com.shipflow.tracking.domain.TrackingEvent"><constructor><idArg column="id" javaType="long"/><arg column="tracking_no" javaType="string"/><arg column="event_code" javaType="string"/><arg column="event_description" javaType="string"/><arg column="event_time" javaType="java.time.LocalDateTime"/><arg column="process_status" javaType="string"/></constructor></resultMap><select id="orderExists" resultType="boolean">SELECT EXISTS(SELECT 1 FROM shipment_order WHERE tenant_id=#{tenantId} AND id=#{orderId})</select><select id="currentStatus" resultType="string">SELECT current_status FROM shipment_order WHERE tenant_id=#{tenantId} AND id=#{orderId}</select><select id="findEvents" resultMap="event">SELECT id,tracking_no,event_code,event_description,event_time,process_status FROM tracking_event WHERE tenant_id=#{tenantId} AND shipment_order_id=#{orderId} ORDER BY event_time ASC,id ASC</select><select id="findLatest" resultMap="event">SELECT id,tracking_no,event_code,event_description,event_time,process_status FROM tracking_event WHERE tenant_id=#{tenantId} AND shipment_order_id=#{orderId} ORDER BY event_time DESC,id DESC LIMIT 1</select></mapper>
