<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.shipflow.store.mapper.StoreAuditMapper"><insert id="insert">INSERT INTO audit_log (tenant_id,operator_user_id,action_type,resource_type,resource_id,request_id,result_status,occurred_at) VALUES (#{tenantId},#{operatorUserId},#{actionType},'store',#{resourceId},#{requestId},'SUCCESS',#{occurredAt})</insert></mapper>
