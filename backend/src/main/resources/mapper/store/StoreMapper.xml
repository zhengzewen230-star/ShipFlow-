<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.shipflow.store.mapper.StoreMapper"><resultMap id="m" type="com.shipflow.store.domain.model.Store"><constructor><idArg column="id" javaType="java.lang.Long"/><arg column="tenant_id" javaType="java.lang.Long"/><arg column="store_code" javaType="string"/><arg column="store_name" javaType="string"/><arg column="platform_code" javaType="string"/><arg column="platform_account" javaType="string"/><arg column="status" javaType="string"/><arg column="version" javaType="_long"/><arg column="created_at" javaType="java.time.LocalDateTime"/><arg column="updated_at" javaType="java.time.LocalDateTime"/></constructor></resultMap><sql id="c">id,tenant_id,store_code,store_name,platform_code,platform_account,status,version,created_at,updated_at</sql><select id="findById" resultMap="m">SELECT <include refid="c"/> FROM merchant_store WHERE id=#{storeId} AND tenant_id=#{tenantId} AND deleted=0</select><select id="findByCode" resultMap="m">SELECT <include refid="c"/> FROM merchant_store WHERE tenant_id=#{tenantId} AND store_code=#{storeCode} AND deleted=0</select><select id="findByAccount" resultMap="m">SELECT <include refid="c"/> FROM merchant_store WHERE tenant_id=#{tenantId} AND platform_code=#{platformCode} AND platform_account=#{platformAccount} AND deleted=0</select><select id="page" resultMap="m">SELECT <include refid="c"/> FROM merchant_store WHERE tenant_id=#{tenantId} AND deleted=0 AND (#{status} IS NULL OR status=#{status}) AND (#{platformCode} IS NULL OR platform_code=#{platformCode}) ORDER BY id LIMIT #{offset},#{pageSize}</select><select id="count" resultType="long">SELECT COUNT(id) FROM merchant_store WHERE tenant_id=#{tenantId} AND deleted=0 AND (#{status} IS NULL OR status=#{status}) AND (#{platformCode} IS NULL OR platform_code=#{platformCode})</select><insert id="insert">INSERT INTO merchant_store (tenant_id,store_code,store_name,platform_code,platform_account,status,deleted,version) VALUES (#{tenantId},#{storeCode},#{storeName},#{platformCode},#{platformAccount},'ACTIVE',0,0)</insert><update id="update">UPDATE merchant_store SET store_name=#{storeName},platform_code=#{platformCode},platform_account=#{platformAccount},version=version+1 WHERE id=#{storeId} AND tenant_id=#{tenantId} AND deleted=0 AND version=#{version}</update><update id="updateStatus">UPDATE merchant_store SET status=#{status},version=version+1 WHERE id=#{storeId} AND tenant_id=#{tenantId} AND deleted=0 AND version=#{version}</update></mapper>
